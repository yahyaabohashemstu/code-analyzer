#include <stdio.h>int main() {     char *str1 = "Bu bir metin";     char *str2;     int length = 0;     while (*(str1 + length) != '\0') {         length++;     }     str2 = (char *) malloc(length + 1);     for (int i = 0; i < length; i++) {         *(str2 + i) = *(str1 + i);     }     *(str2 + length) = '\0';     printf("Kopyalanan metin: %s\n", str2);     free(str2);     return 0; }